# Example Pulumi stack config for `infra/pulumi/`.
#
# Copy to `Pulumi.staging.yaml` (ignored by git).
# See `infra/pulumi/config.ts` for required values and secrets.

config:
  aws:region: us-east-1

  breeze-infra:environment: staging
  breeze-infra:deploymentProfile: cloud
  breeze-infra:appUrl: https://staging.example.com
  breeze-infra:apiUrl: https://staging.example.com
  breeze-infra:imageTag: latest

